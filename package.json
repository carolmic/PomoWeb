{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix pomodoro-app\" \"npm run dev --prefix pomodoro-api\"",
    "build": "npm run build --prefix pomodoro-app"
  },
  "dependencies": {
    "react-icons": "^5.2.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
